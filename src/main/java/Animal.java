public class Animal {
    public int nose = 1;
    int a = 20;

    // Модификатор доступности (public, protected, default (пустое место), private)
    // статичность / non-static (static, non-static (пустое место))
    // тип возвращаемых данных (int, double, String, Scanner, Animal; void)
    // название метода (всегда с маленькой буквы, camelCase)
    // Параметры метода (то, что метод принимает и с чем мы можем или хотим работать)
    void method1() {

        int w = 20;
    }
}
